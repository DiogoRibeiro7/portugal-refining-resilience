# Metrics policy

All scalar results used in the report must be persisted here rather than copied manually from notebook output.

Recommended columns are: `metric`, `product`, `period`, `value`, `unit`, `method`, `source_dataset`, `status`.

Regression results should be stored in tidy coefficient tables plus a compact JSON with model diagnostics.
